{
  "name": "alien-encounters-site",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "csv-parser": "^3.2.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "jest": "^29.6.1",
    "puppeteer": "^24.16.2",
    "start-server-and-test": "^2.0.13"
  },
  "scripts": {
    "serve": "http-server -p 8000 -c-1",
    "smoke": "node ./test/smoke.test.js",
    "test:run": "jest --runInBand",
    "test": "npx start-server-and-test \"npm run serve\" http://127.0.0.1:8000 \"npm run test:run\""
  }
}
